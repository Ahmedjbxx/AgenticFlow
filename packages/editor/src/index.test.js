import { describe, it, expect } from 'vitest';

describe('@agenticflow/editor', () => {
  it('should be a valid package', () => {
    expect(true).toBe(true);
  });

  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });
}); 