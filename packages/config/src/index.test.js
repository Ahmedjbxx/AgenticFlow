describe('@agenticflow/config', () => {
  it('should be a valid package', () => {
    // Basic test to ensure the package structure is valid
    expect(true).toBe(true);
  });
  
  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });
}); 