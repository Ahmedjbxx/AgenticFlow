describe('@agenticflow/types', () => {
  it('should be a valid package', () => {
    expect(true).toBe(true);
  });

  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });
}); 